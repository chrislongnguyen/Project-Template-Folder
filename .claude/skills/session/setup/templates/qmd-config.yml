# QMD Collection Configuration for Memory Vault
# Written by /setup — edit vault paths as needed
collections:
  sessions:
    path: ${VAULT_PATH}/07-Claude/sessions
    pattern: "**/*.md"
  conversations:
    path: ${VAULT_PATH}/07-Claude/conversations
    pattern: "**/*.md"
  daily:
    path: ${VAULT_PATH}/data/processed/daily
    pattern: "**/*.md"
  decisions:
    path: ${VAULT_PATH}/decisions
    pattern: "**/*.md"
