# Environment Configuration Template
# Copy to .env.development / .env.staging / .env.production
# NEVER commit real values — this file is a template only

# Application
APP_NAME=
APP_ENV=development
APP_PORT=3000
APP_LOG_LEVEL=info

# Database
DB_HOST=
DB_PORT=
DB_NAME=
DB_USER=
DB_PASSWORD=

# External APIs
API_KEY=
API_SECRET=

# Security
JWT_SECRET=
ENCRYPTION_KEY=

# Classification: RESTRICTED — Real values must be stored in Proton Pass
