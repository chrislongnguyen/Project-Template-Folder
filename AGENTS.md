# AGENTS.md — {PROJECT_NAME}

> Universal agent rules (AAIF standard). Readable by Claude Code, Cursor, Gemini CLI, and other AI tools.
> Keep minimal — Claude Code reads both CLAUDE.md and AGENTS.md, so overlap doubles token cost.

## Build

- Install: `{npm install / pip install -r requirements.txt / etc.}`
- Test: `{npm test / pytest / etc.}`
- Lint: `{npm run lint / ruff check / etc.}`
- Build: `{npm run build / etc.}`

## Core Conventions

- {Add conventions that ALL AI tools in your team should follow}
