---
# Learn Input Template — Phase 0 Interview Output
# Created by /learn:input command. All downstream commands read from this file.
# Reference: ESD §3.0 (Workstream Boundary), D24, D28, D39
version: "1.0"
---

# Learn Input: {system_name}

_Generated by `/learn:input` on {date}_

---

## 1. System Identity

- **system_name:** {Human-readable name, e.g. "Data Foundation Layer"}
- **subject_abbreviation:** {Short uppercase abbreviation for CAG tag prefix, e.g. "DF" or "AI-UEDS". Used in table CAG tags as Eff.{ABBREV}(R).}
- **eo:** {Effective Outcome — the state this system aims to achieve, technology-agnostic}

## 2. Personas

- **user_persona_r:** {Who does the work (Responsible). Role, context, capability level, job-to-be-done, constraints}
- **user_persona_a:** {Who owns the outcome (Accountable). Role, context, decision authority, constraints}
- **anti_persona:** {Who this system is explicitly NOT for. Specific characteristics that disqualify an actor}

## 3. Input Contract (ESD §3.0 — D39)

What this system receives from upstream. All 6 fields required per D39.

| Field | Value |
|-------|-------|
| **source** | {Upstream system or workstream providing the input, e.g. "SYS-D (Prioritization)"} |
| **schema** | {Data shape — JSON Schema, Pydantic model, or structural description} |
| **validation** | {Rules the payload must satisfy: types, ranges, required fields} |
| **error** | {Behaviour when contract is violated or source unavailable} |
| **sla** | {Availability and latency expectations from upstream} |
| **version** | {Contract version; breaking changes require upstream notification} |

## 4. Output Contract (ESD §3.0 — D39)

What this system produces for downstream. All 6 fields required per D39.

| Field | Value |
|-------|-------|
| **consumer** | {Downstream system or workstream consuming the output, e.g. "SYS-E1 (Scope Management)"} |
| **schema** | {Data shape this system produces; must match consumer's input contract} |
| **validation** | {Rules this system guarantees on the payload} |
| **error** | {Behaviour when this system cannot produce valid output} |
| **sla** | {Availability and latency this system promises to the consumer} |
| **version** | {Contract version; breaking changes require downstream notification} |

## 5. Topic Depth

Select one. This determines how many topics `/learn:research` and `/learn:structure` will process.

- [ ] **T0** — Overview only (1 topic: T0 Overview & Summary)
- [ ] **T0-T2** — Overview + UBS + UDS (3 topics)
- [ ] **T0-T5** — Full depth: Overview + UBS + UDS + EP + EOE/EOT + EOP (6 topics)

**Selected:** {T0 | T0-T2 | T0-T5}

### Topics (auto-populated from depth)

<!-- T0 depth: 1 topic -->
<!-- T0-T2 depth: 3 topics -->
<!-- T0-T5 depth: 6 topics -->

| # | Topic | Pages | Status |
|---|-------|-------|--------|
| T0 | Overview & Summary | P0-P5 | Pending |
| T1 | UBS (Ultimate Blocking System) | P0-P5 | — |
| T2 | UDS (Ultimate Driving System) | P0-P5 | — |
| T3 | EP (Effective Principles) | P0-P5 | — |
| T4 | EOE/EOT (Effective Environment & Tools) | P0-P5 | — |
| T5 | EOP (Effective Operating Procedure) | P0-P5 | — |

_Rows marked "—" are inactive at the selected depth. `/learn:input` removes inactive rows before saving._

## 6. Research Domains

Key domains to research for this system. These guide the deep-research prompt.

1. {Domain 1 — e.g. "Data collection patterns and pipelines"}
2. {Domain 2 — e.g. "Data quality and validation frameworks"}
3. {Domain 3 — e.g. "Data governance and lifecycle management"}
4. {Add more as needed}

---

## Output File

When `/learn:input` saves this completed template, it writes to:

```
learn-input-{system-name-kebab-case}.md
```

Example: `learn-input-data-foundation-layer.md`
