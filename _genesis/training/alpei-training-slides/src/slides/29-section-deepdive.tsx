import { SectionDivider } from '../components/SectionDivider';

export default function SectionDeepDiveSlide() {
  return (
    <SectionDivider
      number={5}
      title="WORK STREAM DEEP DIVE"
      subtitle="Detailed walkthrough of each work stream with commands and templates"
    />
  );
}
